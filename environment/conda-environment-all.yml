name: ponio-complete-env

channels:
  - conda-forge
dependencies:
  # ponio dependencies
  - jinja2
  - sympy
  - numpy
  # build dependencies
  - cmake
  - make
  # visu dependencies
  - scipy
  - matplotlib
  # all examples dependencies
  - eigen
  - samurai
  - cli11
  # tests
  - notebook
  - nbval
  - doctest
  - pytest
